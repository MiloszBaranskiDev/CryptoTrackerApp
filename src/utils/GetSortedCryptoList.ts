const GetSortedCryptoList = (cryptoList: object[], sortBy: string) => {
  // cryptoList?.sort((a: any, b: any) => (a.price > b.price ? 1 : -1));
  return console.log("GetSortedCryptoList");
};

export default GetSortedCryptoList;
